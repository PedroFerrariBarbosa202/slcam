{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug (ST-Link/OpenOCD)",
      "cwd": "${workspaceFolder}",
      "executable": "${workspaceFolder}/app/firmware.elf",
      "request": "launch",
      "type": "cortex-debug",
      "servertype": "openocd",
      "configFiles": [
        "interface/stlink.cfg",
        "target/stm32f1x.cfg"
      ],
      "device": "STM32F401RE"
    }
  ]
}